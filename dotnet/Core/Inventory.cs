// Ищем public static int TryAdd(Player client, nItem item)

    if (sameTypeWeapon != null) return -1;
                }
				
// Вставляем под это 


else if (item.Type == ItemType.MoneyPack)
                {
                    if (isFull(UUID))
                        return -1;
                    return 0;
                }
				
				
// Ищем public static void Add(Player player, nItem item) и в	
if (ClothesItems.Contains(item.Type) || WeaponsItems.Contains(item.Type)
	
добавляем  

|| item.Type == ItemType.MoneyPack

// Ищем ItemsStacks и добавляем

            { ItemType.MoneyPack, 1 },
			
	
// Ищем ItemsRotOffset и добавляем

            { ItemType.MoneyPack, new Vector3() },
			
// Ищем ItemsPosOffset и добавляем

            { ItemType.MoneyPack, new Vector3(0, 0, -0.9) },
			
// Ищем ItemModels и добавляем

            { ItemType.MoneyPack, NAPI.Util.GetHashKey("prop_anim_cash_pile_02") },
			
// ищем 
            {199, "GiveBox" },
			
// и после вставляем

            {8901, "Пачка денег" },
